version: '3.8'
services:
  customerservice:
    build: .
    ports:
      - "5002:80"
    env_file:
      - ./local.env
    # Do not start a Postgres container here — the app will connect to your local DB using the
    # ConnectionStrings__DefaultConnection value defined in local.env (for example using host.docker.internal).

